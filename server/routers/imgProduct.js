const express = require("express");
const router = express.Router();

// description: phần của product
// router.post("/img-product", create);
// router.get("/colors", list);
// router.delete("/color/:id", remove);

module.exports = router;
